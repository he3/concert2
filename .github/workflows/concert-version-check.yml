# ⚠️ AUTO-GENERATED BY CONCERT — DO NOT EDIT
# This file is managed by Concert and will be overwritten on `concert update`.
# Any manual changes will be lost. To customize behavior, see docs/concert/README.md
name: Concert Version Check
on:
  schedule:
    - cron: '0 9 * * 1'  # Weekly on Monday at 9am UTC
  workflow_dispatch:

jobs:
  check-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Install Concert
        run: go install github.com/he3-org/concert/cmd/concert@latest
      - name: Check for updates
        run: concert check-update
